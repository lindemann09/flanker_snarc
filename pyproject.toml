[project]
name = "exp"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "expyriment>=1.0.1",
    "pip>=26.1",
    "requests>=2.33.1",
]

# brew install sdl2 sdl2_image sdl2_mixer sdl2_ttf pkg-config
